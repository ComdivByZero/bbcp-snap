diff --git a/BlackBox/run-BlackBox b/BlackBox/run-BlackBox
index 3f94cf5..e7ec93f 100755
--- a/BlackBox/run-BlackBox
+++ b/BlackBox/run-BlackBox
@@ -5,7 +5,7 @@
 
 rn=`readlink -f "${0}"`
 standardDir=`dirname "${rn}"`
-useDir=`readlink -f .`
+useDir="$SNAP_USER_COMMON"
 
 exe="blackbox"
 
@@ -14,6 +14,11 @@ unset UBUNTU_MENUPROXY
 unset GTK_MODULES
 export LIBOVERLAY_SCROLLBAR=0
 
+if [ ! -f $SNAP_USER_DATA/inited ]; then
+	touch $SNAP_USER_DATA/inited
+	notify-send "BlackBox WARNING" "First loading may be too slow"
+fi
+
 # export LD_DEBUG=all
 exec env \
 	BB_STANDARD_DIR="${standardDir}" BB_USE_DIR="${useDir}" \
