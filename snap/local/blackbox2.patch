diff --git a/run-BlackBox b/run-BlackBox
index 3aecbe8..3a5bd96 100755
--- a/run-BlackBox
+++ b/run-BlackBox
@@ -5,7 +5,7 @@
 
 rn=`readlink -f "${0}"`
 standardDir=`dirname "${rn}"`
-useDir=`readlink -f .`
+useDir="$SNAP_REAL_HOME/blackbox"
 
 exe="blackbox"
 
@@ -14,6 +14,20 @@ unset UBUNTU_MENUPROXY
 unset GTK_MODULES
 export LIBOVERLAY_SCROLLBAR=0
 
+if [ -f $SNAP_USER_DATA/workdir2 ]; then
+	useDir=`cat $SNAP_USER_DATA/workdir2`
+else
+	notify-send "BlackBox WARNING" "First loading may be too slow"
+
+	echo "$useDir" > $SNAP_USER_DATA/workdir2
+	mkdir -p "$useDir"
+	ln -s `realpath $SNAP/..`/current/usr/lib/blackbox2 "$useDir"/blackbox-core
+fi
+
+if [ "$PWD" = "$SNAP_REAL_HOME" ]; then
+	cd "$useDir"
+fi
+
 # export LD_DEBUG=all
 exec env \
 	BB_STANDARD_DIR="${standardDir}" BB_USE_DIR="${useDir}" \
